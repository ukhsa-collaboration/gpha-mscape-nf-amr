nextflow_process {

    name "Test Process RUN_ABRICATE_DB"
    script "modules/local/abricate.nf"
    process "RUN_ABRICATE_DB"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of(
                    tuple(
                        "climb-id",
                        file("${projectDir}/tests/test_data/ERR3077524_GCF_013349085.1.test_data.5k_reads.fq", checkIfExists: true),
                        "card"
                    )
                )
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
