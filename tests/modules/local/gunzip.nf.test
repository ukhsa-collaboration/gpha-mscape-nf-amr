nextflow_process {

    name "Test Process GZ_TO_FASTQ"
    script "modules/local/gunzip.nf"
    process "GZ_TO_FASTQ"

    test("Should run gunzip without any failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = Channel.of(
                    tuple(
                        "climb-id",
                        file("${projectDir}/tests/test_data/ERR3077524_GCF_013349085.1_1x.fq.gz")
                    )
                )
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
