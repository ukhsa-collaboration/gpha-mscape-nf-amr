nextflow_process {

    name "Test Process RUN_ABRICATE_DB"
    script "modules/local/abricate.nf"
    process "RUN_ABRICATE_DB"

    test("Should run Abricate") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = val("test-climb-id") // Climb ID
                input[1] = file("test/test_data/ERR3077524_GCF_013349085.1_1x_kraken_pluspf_8gb.tsv") // Kraken Assignment file
                input[2] = file("test/test_data/ERR3077524_GCF_013349085.1_1x_kraken_pluspf_8gb.report.json") // Kraken Report File
                input[3] = file("test/test_data/ERR3077524_GCF_013349085.1_1x.fq.gz") // FASTQ Fukes
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
